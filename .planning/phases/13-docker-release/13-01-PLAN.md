---
phase: 17-docker-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - .env.example
  - README.md
autonomous: true

must_haves:
  truths:
    - "User can find comprehensive documentation on GitHub"
    - "User can follow installation steps to set up klausbot"
    - "User can reference all CLI and Telegram commands"
    - "User can troubleshoot common issues using FAQ"
    - "User can verify project license from README badge"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "TELEGRAM_BOT_TOKEN"
    - path: "README.md"
      provides: "Comprehensive project documentation"
      min_lines: 200
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "badge link"
      pattern: "\\[!\\[License.*\\]\\(LICENSE\\)"
    - from: "README.md"
      to: ".env.example"
      via: "installation reference"
      pattern: "\\.env\\.example"
---

<objective>
Create comprehensive project documentation including README, LICENSE, and environment template.

Purpose: Users can discover, install, configure, and troubleshoot klausbot from GitHub.
Output: LICENSE, .env.example, README.md with all sections except personal narrative (user fills later).
</objective>

<execution_context>
@/home/soham/.claude/get-shit-done/workflows/execute-plan.md
@/home/soham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-docker-release/17-CONTEXT.md
@.planning/phases/17-docker-release/17-RESEARCH.md
@src/config/schema.ts
@src/index.ts
@src/telegram/commands.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIT LICENSE file</name>
  <files>LICENSE</files>
  <action>
Create LICENSE file with standard MIT license text.

Use current year (2026) and copyright holder "klausbot contributors" (neutral, common for open source).

Standard MIT text:

```
MIT License

Copyright (c) 2026 klausbot contributors

Permission is hereby granted, free of charge, to any person obtaining a copy
[... standard MIT text ...]
```

WHY MIT: User decision in 17-CONTEXT.md.
</action>
<verify>
`cat LICENSE | head -3` shows "MIT License" and copyright line
</verify>
<done>LICENSE file exists with valid MIT license text</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example template</name>
  <files>.env.example</files>
  <action>
Create .env.example with ALL environment variables documented.

From src/config/schema.ts:

- TELEGRAM_BOT_TOKEN (required)
- OPENAI_API_KEY (optional, for semantic memory search)
- LOG_LEVEL (optional, default: info)

Format per research template:

```bash
# Telegram Bot Token (required)
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Log level (optional, default: info)
# Options: silent, trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# OpenAI API Key (optional)
# Required for semantic memory search
# Get from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=
```

WHY all vars: User decision - "Include ALL vars in .env.example with comments explaining each"
</action>
<verify>
`grep -c "^#" .env.example` shows 6+ comment lines (explanatory comments exist)
`grep "TELEGRAM_BOT_TOKEN" .env.example` finds the required var
</verify>
<done>.env.example contains all vars from schema with helpful comments</done>
</task>

<task type="auto">
  <name>Task 3: Create README structure, story placeholder, and features</name>
  <files>README.md</files>
  <action>
Create README.md with structure from 17-RESEARCH.md and decisions from 17-CONTEXT.md.

**Write these sections:**

1. **Title + Badge**
   - `# klausbot`
   - MIT badge: `[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)`

2. **Screenshot placeholder**
   - `[Screenshot placeholder - klausbot Telegram interaction]`
   - Note: User adds actual screenshots in review checkpoint

3. **Why I Built It** (PLACEHOLDER for user)

   ```markdown
   ## Why I Built It

   <!--
   USER: Fill in your personal narrative here.
   - What problem were you trying to solve?
   - What's your background/journey?
   - What vision does klausbot represent?
   -->

   [Personal narrative to be added]
   ```

4. **What It Does**
   - 2-3 paragraph feature overview
   - Bullet list: 24/7 Claude assistant, persistent memory, scheduled tasks, voice transcription

5. **Installation**
   - Prerequisites: Node.js 20+, Telegram bot token, Claude CLI
   - Quick Start: `npm install -g klausbot && klausbot setup`
   - Docker section: "Coming Soon" (per user decision)

**Important constraints:**

- NO multiple badges (only MIT)
- Docker is "Coming Soon" only
- All config in single README (no separate docs folder)
  </action>
  <verify>
  `grep "MIT" README.md` finds license badge
  `grep "Coming Soon" README.md` finds Docker placeholder
  `grep "klausbot setup" README.md` finds installation command
  </verify>
  <done>
  README.md contains sections:
- Title with MIT badge linking to LICENSE
- Screenshot placeholder
- "Why I Built It" placeholder for user
- Features overview (What It Does)
- Installation with prerequisites and Docker "Coming Soon"
  </done>
  </task>

<task type="auto">
  <name>Task 4: Add README command tables, config, and troubleshooting</name>
  <files>README.md</files>
  <action>
Append to README.md the reference sections.

**Add these sections:**

6. **Usage**
   - CLI Commands table (from src/index.ts):
     | Command | Description |
     |---------|-------------|
     | `klausbot setup` | First-time setup wizard |
     | `klausbot daemon` | Start the gateway daemon |
     | `klausbot status` | Check service and config status |
     | `klausbot restart` | Restart background service |
     | `klausbot uninstall` | Remove background service |
     | `klausbot init` | Reset ~/.klausbot/ directory |
     | `klausbot cron [action] [id]` | Manage scheduled jobs |
     | `klausbot config validate` | Validate environment and config |
     | `klausbot pairing list` | List pending/approved users |
     | `klausbot pairing approve <code>` | Approve pairing request |
     | `klausbot pairing reject <code>` | Reject pairing request |
     | `klausbot pairing revoke <chatId>` | Revoke user access |

   - Telegram Commands table (from src/telegram/commands.ts):
     | Command | Description |
     |---------|-------------|
     | `/start` | Request pairing or check status |
     | `/status` | Show queue and approval status |
     | `/model` | Show current model info |
     | `/crons` | List scheduled tasks |
     | `/help` | Show available commands |

7. **Configuration**
   - Environment Variables table (from schema.ts):
     | Variable | Required | Default | Description |
     |----------|----------|---------|-------------|
     | `TELEGRAM_BOT_TOKEN` | Yes | - | Telegram bot token from @BotFather |
     | `OPENAI_API_KEY` | No | - | OpenAI API key for semantic memory search |
     | `LOG_LEVEL` | No | `info` | Log level (silent/trace/debug/info/warn/error/fatal) |

   - JSON Configuration table (~/.klausbot/config/klausbot.json):
     | Key | Default | Description |
     |-----|---------|-------------|
     | `model` | `claude-sonnet-4-20250514` | AI model for responses |
     | `logVerbosity` | `normal` | Logging detail (minimal/normal/verbose) |
     | `preferences.timezone` | - | Timezone for date/time formatting |
     | `preferences.language` | `en` | Preferred language code |

8. **Troubleshooting** (FAQ format per user decision)
   - "Bot not responding?" - check `klausbot status`, verify token, check logs
   - "Memory search not working?" - OPENAI_API_KEY required
   - "How do I restart after config changes?" - `klausbot restart`
   - "Where are logs?" - Point to `klausbot status` (universal per user decision)
   - Support path: GitHub Issues

9. **License**
   - "MIT - see [LICENSE](LICENSE)"

**Important constraints:**

- NO platform-specific troubleshooting sections
- Use `klausbot status` as universal diagnostic
  </action>
  <verify>
  `wc -l README.md` shows 200+ lines
  `grep "Troubleshooting" README.md` finds FAQ section
  `grep "Configuration" README.md` finds config section
  </verify>
  <done>
  README.md contains all reference sections:
- Usage with CLI and Telegram command tables
- Configuration (env vars + JSON config)
- Troubleshooting FAQ
- License footer
  Total: 200+ lines
  </done>
  </task>

</tasks>

<verification>
All documentation files created and internally consistent:
- `ls LICENSE .env.example README.md` - all 3 files exist
- `grep "LICENSE" README.md` - badge links to LICENSE file
- `grep ".env.example" README.md` - installation references env template
- README contains all required sections per RLSE-01 through RLSE-05
</verification>

<success_criteria>

- LICENSE file exists with MIT text
- .env.example contains all env vars with comments
- README.md has 200+ lines covering all 9 sections:
  1. Title + Badge
  2. Screenshot placeholder
  3. Why I Built It (placeholder)
  4. What It Does
  5. Installation
  6. Usage (command tables)
  7. Configuration
  8. Troubleshooting
  9. License
- "Why I Built It" section has placeholder for user to fill
- Docker section says "Coming Soon"
- No separate docs folder (single file per user decision)
  </success_criteria>

<output>
After completion, create `.planning/phases/17-docker-release/17-01-SUMMARY.md`
</output>
