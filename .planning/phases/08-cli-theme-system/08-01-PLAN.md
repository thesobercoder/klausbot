---
phase: 08-cli-theme-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/theme.ts
autonomous: true

must_haves:
  truths:
    - "Theme module exports all required helper methods"
    - "Helpers output directly to console (not return strings)"
    - "NO_COLOR environment variable is respected"
    - "ASCII art displays klausbot branding"
  artifacts:
    - path: "src/cli/theme.ts"
      provides: "Theme singleton with output helpers"
      exports: ["theme"]
      min_lines: 150
  key_links:
    - from: "src/cli/theme.ts"
      to: "picocolors"
      via: "import"
      pattern: "import pc from 'picocolors'"
---

<objective>
Create CLI theme module with muted aesthetic output helpers

Purpose: Establish unified styling foundation for all CLI output
Output: Theme singleton with success/error/warn/info/header/list/table/keyValue/box/divider/asciiArt helpers
</objective>

<execution_context>
@/home/soham/.claude/get-shit-done/workflows/execute-plan.md
@/home/soham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-theme-system/08-CONTEXT.md

Codebase uses picocolors (not chalk) - use pc for consistency
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme module with muted color helpers</name>
  <files>src/cli/theme.ts</files>
  <action>
Create theme module at src/cli/theme.ts with:

1. Color scheme (muted aesthetic):
   - success: pc.green (muted green already via picocolors)
   - error: pc.red
   - warn: pc.yellow
   - info: pc.cyan
   - muted/dim: pc.dim
   - header: pc.bold + color

2. NO_COLOR support:
   - Check process.env.NO_COLOR at module load
   - If set, all color functions become identity (pass through unchanged)
   - Picocolors already handles this but be explicit for clarity

3. Helper methods that OUTPUT directly (console.log internally):
   - success(msg): green checkmark + message
   - error(msg): red X + message
   - warn(msg): yellow warning + message
   - info(msg): cyan info + message
   - header(title): bold colored header with divider
   - list(items, opts?): bullet list with optional indentation
   - table(rows, opts?): Unicode box-drawing table (─│┌┐└┘├┤┬┴┼)
   - keyValue(key, value): aligned key: value pairs
   - box(content, opts?): bordered box with title
   - divider(char?): horizontal line
   - blank(): empty line

4. ASCII art method:
   - asciiArt(): outputs "klausbot" in stylized text
   - Simple but recognizable style (keep it minimal)
   - Example style:
     ```
     ┌─────────────────────────┐
     │  klausbot               │
     │  Telegram x Claude Code │
     └─────────────────────────┘
     ```

5. Export pattern:
   - Singleton: `export const theme = { ... }`
   - Named export for each method for convenience

Note: All methods should call console.log internally and return void.
</action>
<verify> - `grep -r "export const theme" src/cli/theme.ts` returns match - `grep "NO_COLOR" src/cli/theme.ts` returns match - File has 150+ lines
</verify>
<done>Theme module exists with all helper methods, respects NO_COLOR, exports singleton</done>
</task>

<task type="auto">
  <name>Task 2: Add theme to CLI module exports</name>
  <files>src/cli/index.ts</files>
  <action>
Update src/cli/index.ts to export theme module:

Add: `export { theme } from './theme.js';`

This enables `import { theme } from './cli/index.js'` from other files.
</action>
<verify>`grep "theme" src/cli/index.ts` shows export</verify>
<done>Theme is re-exported from CLI module barrel</done>
</task>

</tasks>

<verification>
1. Theme module compiles: `npm run build` succeeds
2. Theme exports correct shape: check dist/cli/theme.js exists
3. Can import theme: add temporary test in another file
</verification>

<success_criteria>

- src/cli/theme.ts exists with 150+ lines
- All required helpers implemented (success, error, warn, info, header, list, table, keyValue, box, divider, asciiArt)
- NO_COLOR respected
- Theme re-exported from src/cli/index.ts
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cli-theme-system/08-01-SUMMARY.md`
</output>
