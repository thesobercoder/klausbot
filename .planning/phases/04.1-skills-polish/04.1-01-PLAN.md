---
phase: 04.1-skills-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/context.ts
autonomous: true

must_haves:
  truths:
    - "Claude sees skill folder location in every system prompt"
    - "Reminder appears first in prompt (before identity)"
  artifacts:
    - path: "src/memory/context.ts"
      provides: "getSkillReminder() and updated buildSystemPrompt()"
      exports: ["buildSystemPrompt", "getSkillReminder"]
  key_links:
    - from: "buildSystemPrompt()"
      to: "getSkillReminder()"
      via: "prepends to prompt string"
      pattern: "getSkillReminder\\(\\).*identity"
---

<objective>
Add skill folder reminder to system prompt so Claude knows where to save skills.

Purpose: Claude currently doesn't know to save skills in ~/.claude/skills/ — this causes confusion when creating skills.
Output: Every Claude session starts with reminder about skill folder location.
</objective>

<execution_context>
@/home/soham/.claude/get-shit-done/workflows/execute-plan.md
@/home/soham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-polish/04.1-CONTEXT.md
@.planning/phases/04.1-skills-polish/04.1-RESEARCH.md
@src/memory/context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skill folder reminder to system prompt</name>
  <files>src/memory/context.ts</files>
  <action>
Add a new function `getSkillReminder()` that returns a static skill folder reminder.

Content (from CONTEXT.md decision — minimal, location only):

```
<skill-folder>
Skills live in ~/.claude/skills/ — create and save skills there.
</skill-folder>
```

Update `buildSystemPrompt()` to prepend the skill reminder BEFORE identity content:

- `getSkillReminder() + '\n\n' + identity + '\n\n' + instructions`

Why prepend: Decision from CONTEXT.md — "Top of system prompt — first thing Claude sees"

Export `getSkillReminder` for potential testing.
</action>
<verify>
Run TypeScript compilation: `npm run build`
Verify output includes skill reminder at start: `grep -A2 "skill-folder" src/memory/context.ts`
</verify>
<done>
buildSystemPrompt() returns string starting with `<skill-folder>` tag containing ~/.claude/skills/ path
</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. buildSystemPrompt() output starts with `<skill-folder>` tag
3. Skill reminder mentions ~/.claude/skills/ path
4. Reminder appears before identity content in prompt
</verification>

<success_criteria>

- [x] getSkillReminder() returns static skill folder reminder
- [x] buildSystemPrompt() prepends reminder before identity
- [x] Build passes
      </success_criteria>

<output>
After completion, create `.planning/phases/04.1-skills-polish/04.1-01-SUMMARY.md`
</output>
